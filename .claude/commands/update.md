---
description: Creative Canvas を最新版に更新して再起動する
---
このリポジトリ（Creative Canvas）を最新版に更新してください。

1. `git pull` で最新を取得する。競合やローカル変更があれば内容を説明し、どうするか確認する。
2. `npm install` を実行（依存が増減している場合に備える）。
3. `npm run app` で再ビルドして起動する。

完了後、作業データ（プロジェクト・生成物）はユーザーデータ領域に保存されているため更新では消えないこと、APIキーの再設定も不要であることを伝える。
